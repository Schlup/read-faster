// GALLERY, where all the PDFs are stored

import { Text, View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        backgroundColor: "black",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text
        style={{
          color: "red",
        }}
      >
        Configurations and shit.
      </Text>
    </View>
  );
}
